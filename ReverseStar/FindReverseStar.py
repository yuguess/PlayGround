
downTrendLength = 20
followUpLength = 20

def isReverseStart(pn, hi, lo, cls):
  solidLen = abs(pn - cls)
  return  solidLen < h and (hi - max(hi, cls)) > a * solidLen and (min(op, cls) - lo) > b * solidLen

def dumpReverseStar(idx, lines):


for stock in StockList
  lines = [line for line in open()]
  length = len(lines)
  idx = 0
  while (idx < length)
    fields = lines[idx].split()
    if isReverseStar(float(fields[0]), float(fields[1]), float(fields[2]), float(fields[3])):
      if (idx - downTrendLength >= 0 and idx + followUpTrade < length)
        dumpReverseStar(idx, lines)
        print date, T-20, T-1, T+1, T+2,T+3

    idx++

